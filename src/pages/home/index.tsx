import { Box } from "@mui/material";
import ProjectsWheel from "../projects/projects-wheel";

const Home: React.FC = () => {
  return (
    <Box maxHeight="100vh">
      <ProjectsWheel />
    </Box>
  );
};

export default Home;
